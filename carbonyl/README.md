# Carbonyl

Managed notes for using `carbonyl` in this setup.

Related docs: [README.md](../README.md), [config-docs.md](../config-docs.md)

- Binary: `carbonyl`
- Role: terminal-native browser for quick docs/web inspection inside tmux/zellij/ssh workflows

Install and verify:

```bash
command -v carbonyl
carbonyl --help
```
